# ============================================================
# 核心运行时依赖源文件
# 使用 pip-compile 生成锁定的 requirements.txt
# 命令: pip-compile requirements.in -o requirements.txt
# ============================================================

# 异步支持
asyncio-pool>=0.6.0
aiofiles>=23.2.1

# 数据模型与验证
pydantic>=2.5.0

# 进程管理
psutil>=5.9.0

# 日志
loguru>=0.7.2

# 配置管理
python-dotenv>=1.0.0
pyyaml>=6.0.1

# 类型支持
typing-extensions>=4.9.0

# HTTP 客户端 (核心通信依赖)
httpx>=0.27.0           # 异步 HTTP 客户端，Cloud API 通信
websockets>=12.0        # WebSocket 实时连接支持

# ============================================================
# 注意: 以下依赖已移至可选分组
# ============================================================
# [web] 网页处理 → requirements-optional.txt / pyproject.toml [web]
#   beautifulsoup4, html2text, lxml
#
# [ml] ML/向量搜索 → requirements-optional.txt / pyproject.toml [ml]
#   sentence-transformers, chromadb, tiktoken, hnswlib
